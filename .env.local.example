# Telegram Bot API — the ONLY place these values live (server-side env).
# Create a bot with @BotFather, get your token, and add the bot to the
# @penaplast_uz chat (or use the chat id of the account receiving orders).
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
